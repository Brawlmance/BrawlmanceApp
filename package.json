{
  "name": "app",
  "scripts": {
    "update": "git clean -df && git checkout -- . && git pull && cd packages/web && npm run build && cd ../.. && pm2 reload BrawlmanceAPI && pm2 reload BrawlmanceWeb"
  }
}
